# Disables source map generation entirely.
# This eliminates the @zxing "Failed to parse source map" warning because
# react-scripts' source-map-loader never runs when source maps are off.
GENERATE_SOURCEMAP=false
REACT_APP_ADMIN_EMAIL=krishanhimesh@gmail.com